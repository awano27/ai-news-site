# Dependencies for tool ingest pipeline
# Install with: pip install -r requirements-ingest.txt

# HTTP requests with retry support
requests>=2.28.0

# URL parsing and validation (stdlib, but documenting)
# urllib3 is installed as requests dependency

# JSON schema validation (optional, for strict validation)
jsonschema>=4.17.0

# Text similarity for deduplication (optional, using stdlib difflib instead)
# rapidfuzz>=3.0.0  # Uncomment for faster fuzzy matching

# Testing
pytest>=7.4.0
