# Daily AI News System v2.0 - Python Dependencies (Python 3.13 Compatible)

# Core dependencies
feedparser==6.0.11
trafilatura==1.12.0
beautifulsoup4==4.12.3
tldextract==5.1.2
python-dateutil==2.9.0
requests==2.32.3
PyYAML==6.0.2

# AI/ML dependencies (Python 3.13 compatible versions)
google-generativeai==0.8.3

# Simplified ML stack for Python 3.13
# sentence-transformers # Commented out - causing issues with Python 3.13
# scikit-learn # Commented out - causing build issues
numpy>=1.24.0,<2.0.0  # Stable version compatible with Python 3.13

# NLP dependencies - simplified
# spacy>=3.7.0  # Commented out - can be added later
# transformers>=4.36.0  # Commented out - can be added later

# Search and retrieval - pure Python implementation
# rank-bm25==0.2.2  # Will implement BM25 internally

# Database dependencies (optional for now)
# psycopg2-binary==2.9.9  # PostgreSQL - can be added when needed
# redis==5.0.4  # Can be added when needed

# Data processing
# pandas>=2.0.0  # Can be added when needed
# polars>=0.20.0  # Can be added when needed

# Web scraping and HTTP
httpx==0.27.0
# playwright==1.43.0  # Heavy dependency - can be added later

# Async support (built-in modules mostly)
aiohttp==3.9.5
aiofiles==23.2.0

# Monitoring and logging
structlog==24.1.0
# sentry-sdk==2.1.1  # Can be added later

# Configuration and environment
pydantic>=2.0.0
python-dotenv==1.0.1

# Testing dependencies (optional for now)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# pytest-cov>=4.0.0
# pytest-mock>=3.14.0

# Development dependencies (optional)
# black>=24.0.0
# isort>=5.13.0
# flake8>=7.0.0
# mypy>=1.9.0

# Deployment and packaging (when needed)
# gunicorn>=22.0.0
# uvicorn>=0.29.0